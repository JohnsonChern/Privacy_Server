Demo app for [FHIR Genomics API](https://github.com/dsrcl/fhir-genomics).

Dependencies: `requests` and `flask`. If you have `pip`, simply do
```
$ pip install requests flask
```
Simply run the server at localhost with:
```
$ python app.py
```
